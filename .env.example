DATABASE_URL=./data/app.db
NEXT_PUBLIC_APP_NAME=个人成长台

# Comma-separated CORS origins (Caddy URL + Capacitor schemes)
ALLOWED_ORIGINS=https://your-domain.com

# --- Phase 4 (push) — leave blank for now ---
# FCM_SERVICE_ACCOUNT_JSON_PATH=/run/secrets/fcm.json
# PUSH_CHECK_INTERVAL_MS=60000

# --- local dev on Windows: set NEXT_STANDALONE=1 to enable standalone build ---
# (Docker build sets it automatically)